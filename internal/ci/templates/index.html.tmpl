<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Prompt Story - PR #{{.PRNumber}}</title>
  <style>{{.CSS}}</style>
</head>
<body>
  <div class="header">
    <h1>Prompt Story</h1>
    <p class="meta">PR #{{.PRNumber}} | {{.CommitsWithNotes}} commit(s) with LLM sessions</p>
  </div>

  <div class="stats">
    <div class="stat">
      <div class="stat-value">{{.CommitsAnalyzed}}</div>
      <div class="stat-label">Commits Analyzed</div>
    </div>
    <div class="stat">
      <div class="stat-value">{{.CommitsWithNotes}}</div>
      <div class="stat-label">With Sessions</div>
    </div>
    <div class="stat">
      <div class="stat-value">{{.TotalPrompts}}</div>
      <div class="stat-label">Total Prompts</div>
    </div>
  </div>

  {{if .Commits}}
  <h2>Commits</h2>
  <table>
    <thead>
      <tr>
        <th>Commit</th>
        <th>Subject</th>
        <th>Tool</th>
        <th>Prompts</th>
        <th>Details</th>
      </tr>
    </thead>
    <tbody>
      {{range .Commits}}
      <tr>
        <td><code>{{.ShortSHA}}</code></td>
        <td>{{truncate .Subject 50}}</td>
        <td>{{.ToolNames}}</td>
        <td>{{.PromptCount}}</td>
        <td><a href="{{.ShortSHA}}.html">View transcript</a></td>
      </tr>
      {{end}}
    </tbody>
  </table>

  <h2>Session Details</h2>
  {{range .Commits}}
  <div class="commit-card">
    <div class="commit-header">
      <h3><code>{{.ShortSHA}}</code> {{.Subject}}</h3>
      <div class="commit-meta">
        Work period: {{formatTime .StartWork}} - {{formatTime .EndWork}}
      </div>
    </div>
    {{range .Sessions}}
    <div class="session">
      <div class="session-header">
        {{formatToolName .Tool}} | {{formatTime .Start}} - {{formatTime .End}} | {{len .Prompts}} prompt(s)
      </div>
      <ul class="prompt-list">
        {{range .Prompts}}
        <li class="prompt-item {{.Type}}">
          <span class="prompt-time">{{formatTimeShort .Time}}</span>
          <span class="prompt-type">{{.Type}}</span>
          <span class="prompt-text{{if .Truncated}} truncated{{end}}">{{.Text}}</span>
        </li>
        {{end}}
      </ul>
    </div>
    {{end}}
  </div>
  {{end}}
  {{else}}
  <p>No prompt-story notes found in this PR.</p>
  {{end}}

  <div class="footer">
    Generated by <a href="https://github.com/QuesmaOrg/git-prompt-story">git-prompt-story</a>
  </div>
</body>
</html>
